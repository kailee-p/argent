import React from 'react';
import AsyncSelect from 'react-select/async';

const SearchBar = () => {

  return (
    <AsyncSelect 
      cacheOptions 
      defaultOptions />
  )
}

export default SearchBar;
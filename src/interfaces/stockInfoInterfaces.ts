export interface lastQuoteInterface {
  askPrice: number,
  bidPrice: number,
  spread: number
}

export interface prevDayInfoInterface {
  closePrice: number,
  highestPrice: number,
  lowestPrice: number,
  openPrice: number
}
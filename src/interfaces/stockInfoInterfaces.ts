export interface lastQuoteInterface {
  askPrice: string,
  bidPrice: string,
  spread: string
}

export interface prevDayInfoInterface {
  closePrice: string,
  highestPrice: string,
  lowestPrice: string,
  openPrice: string
}